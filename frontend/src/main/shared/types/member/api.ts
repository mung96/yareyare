export type MemberResponse = {
  name: string;
  birth: string;
  tel: string;
  email: string;
  myTeamId: string;
  myTeamName: string;
  isCertificated: boolean;
  uuid: string;
};

export type MyTeamResponse = {
  myTeamId: string;
};
